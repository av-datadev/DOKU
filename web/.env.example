# Public — safe to ship to the browser (same anon key already in the live site).
PUBLIC_SUPABASE_URL=https://mudyipmizlvihcldzrvh.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJ...anon-key...

# Server-only — signs the cart cookie. Any long random string; never shipped
# to the browser. In production set it as a Cloudflare Worker secret, not here.
CART_SECRET=change-me-to-a-long-random-string
